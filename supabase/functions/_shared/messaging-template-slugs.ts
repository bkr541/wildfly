// Canonical slugs for all messaging templates.
// Use these constants in Edge Functions instead of raw string literals.
export const MESSAGING_TEMPLATE_SLUGS = {
  BETA_APPLICATION_ACCEPTED: "beta-application-accepted",
} as const;
